<div class="hero-section text-center text-white py-5 mb-5" 
     style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 25px; 
            min-height: 70vh;
            background-image: radial-gradient(circle at center, rgba(255,255,255,0.1), transparent 60%);">
  
  <div class="container h-100 d-flex flex-column justify-content-center">
    <div class="row">
      <div class="col-lg-8 mx-auto">
        <h1 class="display-3 fw-bold mb-4 animate-fade-in">
          🎮 Tu Colección<br>de Videojuegos
        </h1>
        <p class="lead mb-4 opacity-90 fs-4">
          Gestiona, filtra y organiza tu biblioteca personal.
        </p>
        
        <% if (!username) { %>
          <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
            <a href="/register" class="btn btn-light btn-lg px-5 py-3 fs-5">
              <i class="bi bi-person-plus me-2"></i>Crear cuenta gratis
            </a>
            <a href="/login" class="btn btn-outline-light btn-lg px-5 py-3 fs-5">
              <i class="bi bi-box-arrow-in-right me-2"></i>Iniciar sesión
            </a>
          </div>
        <% } else { %>
          <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
            <a href="/videojuegos" class="btn btn-light btn-lg px-5 py-3 fs-5">
              <i class="bi bi-collection-play me-2"></i>Ver mis <%= juegosCount || 'juegos' %>
            </a>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</div>

<!--si logueado -->
<% if (username) { %>
<div class="row g-4 mb-5">
  <div class="col-md-4">
    <div class="card h-100 text-center p-4">
      <i class="bi bi-clock display-4 text-primary mb-3"></i>
      <h3 class="fw-bold"><%= stats.pendientes || 0 %></h3>
      <p class="text-muted">Pendientes</p>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card h-100 text-center p-4">
      <i class="bi bi-play-circle display-4 text-warning mb-3"></i>
      <h3 class="fw-bold"><%= stats.jugando || 0 %></h3>
      <p class="text-muted">Jugando</p>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card h-100 text-center p-4">
      <i class="bi bi-check-circle display-4 text-success mb-3"></i>
      <h3 class="fw-bold"><%= stats.completados || 0 %></h3>
      <p class="text-muted">Completados</p>
    </div>
  </div>
</div>
<% } %>